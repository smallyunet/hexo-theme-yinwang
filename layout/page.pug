extends includes/layout.pug

block content
  .inner
    h2= page.title
    if page.type == 'tags'
      if site.tags && site.tags.length
        != tagcloud()
        h3 标签列表
        != list_tags()
        != page.content
    else 
      != page.content
